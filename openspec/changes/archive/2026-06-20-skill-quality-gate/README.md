# skill-quality-gate

Quality gate: every funbox skill carries committed eval inputs + a hash-verified benchmark; an author-time command produces it via skill-creator's engine, CI enforces pass-rate >= threshold.
