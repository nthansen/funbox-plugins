{
  "skill": "install-docs-ci",
  "pass_rate": 1.0,
  "threshold": 0.9,
  "model": "claude-opus-4-8[1m]",
  "source_hash": "sha256:6758f60891eca9c34eab66159bbc99a4c54712c2bf8416780ff0dc8be6b1e3d6",
  "results": [
    { "eval_id": 1, "text": "Vendors the check script docs-ci-check.sh into the project's .github/doc-sweep/ directory", "passed": true, "evidence": "cp bundled docs-ci-check.sh to .github/doc-sweep/; present, executable, LF" },
    { "eval_id": 1, "text": "Also copies doc-classify.mjs alongside docs-ci-check.sh into .github/doc-sweep/ so the classifier resolves at runtime", "passed": true, "evidence": "doc-classify.mjs copied to same dir; end-to-end run of docs-ci-check.sh invoked $here/doc-classify.mjs with no fail-open warning" },
    { "eval_id": 1, "text": "For the default doc-file set, writes a config JSON at .github/doc-sweep/docs-ci.json that OMITS docPatterns (the classifier's built-in default applies) and records an excludeDirs array", "passed": true, "evidence": "docs-ci.json = {\"excludeDirs\":[]} — no docPatterns key" },
    { "eval_id": 1, "text": "Scaffolds a pull_request workflow at .github/workflows/doc-sweep-docs.yml that runs the vendored check script with checkout fetch-depth:0", "passed": true, "evidence": "workflow has on:pull_request, fetch-depth:0, run: bash .github/doc-sweep/docs-ci-check.sh .github/doc-sweep/docs-ci.json; PR-sim: code-only fails, +README passes" },
    { "eval_id": 1, "text": "Prints a structured summary listing the workflow/script/classifier/config paths, the doc-file set, the [skip docs] token, the branch-protection note, and how to edit/uninstall", "passed": true, "evidence": "step-6 template filled with actual paths + default choices; all required fields present" },
    { "eval_id": 1, "text": "Does not scaffold anything until the user confirms", "passed": true, "evidence": "step 2 (AskUserQuestion) precedes the only write steps (4/5); 'Nothing is installed until you run this and confirm'" },
    { "eval_id": 1, "text": "Re-running the install does not duplicate the workflow (idempotent)", "passed": true, "evidence": "md5 of workflow identical before/after; find count stayed 1" },
    { "eval_id": 1, "text": "For the default exempt-set choice, omits exemptPatterns from the config JSON so the classifier's built-in test globs apply", "passed": true, "evidence": "docs-ci.json has no exemptPatterns key; classifier falls back to DEFAULT_EXEMPT_PATTERNS" },
    { "eval_id": 2, "text": "Deletes the scaffolded workflow .github/workflows/doc-sweep-docs.yml", "passed": true, "evidence": "post-uninstall find shows the workflow gone" },
    { "eval_id": 2, "text": "Deletes the vendored check script, the vendored doc-classify.mjs, and the docs-ci.json config under .github/doc-sweep/", "passed": true, "evidence": "all three rm -f'd; .github/doc-sweep/ rmdir'd (was empty)" },
    { "eval_id": 2, "text": "Leaves unrelated workflows and files untouched", "passed": true, "evidence": "pre-created ci.yml + marker file remained byte-identical after uninstall" },
    { "eval_id": 2, "text": "Confirms what was removed (workflow, script, classifier, config paths)", "passed": true, "evidence": "Uninstall section mandates confirming the four paths; all four named/tracked" },
    { "eval_id": 3, "text": "Detects the existing install and offers Reconfigure / Uninstall / Cancel rather than a blind fresh install", "passed": true, "evidence": "detection test -f workflow -a -f script succeeded → Reconfigure branch" },
    { "eval_id": 3, "text": "On Reconfigure, rewrites the docs-ci.json config with docPatterns set to the minimal glob list (e.g. CLAUDE*.md and README*.md only) while preserving the other choices", "passed": true, "evidence": "post docs-ci.json = {docPatterns:[**/CLAUDE*.md,**/README*.md], excludeDirs:[legacy]}; excludeDirs preserved" },
    { "eval_id": 3, "text": "Leaves the workflow file in place (rewrites it only if its path or name changed)", "passed": true, "evidence": "md5 of workflow identical before/after reconfigure" },
    { "eval_id": 3, "text": "On Reconfigure, unconditionally re-copies doc-classify.mjs alongside docs-ci-check.sh into .github/doc-sweep/ (self-heals a missing or stale classifier) even though the workflow file itself is left in place", "passed": true, "evidence": "stale placeholder classifier → after reconfigure diff -q vs bundled empty (self-healed); workflow untouched" },
    { "eval_id": 4, "text": "Scans for likely-vendored directories (submodules, non-root package manifests, known vendor names) and presents candidates for confirmation", "passed": true, "evidence": "three signals fired: .gitmodules → libs/thirdparty; non-root packages/sub-pkg/package.json (root excluded); vendor/ dir" },
    { "eval_id": 4, "text": "Records the confirmed excludeDirs list into the .github/doc-sweep/docs-ci.json config", "passed": true, "evidence": "docs-ci.json = {excludeDirs:[libs/thirdparty, packages/sub-pkg, vendor]}" },
    { "eval_id": 4, "text": "Reads an existing excludeDirs list from .claude/context/audience-rules.md if one is already present rather than re-prompting", "passed": true, "evidence": "pre-populated audience-rules excludeDirs:[already-recorded-dir]; skill read it (not vendor from a fresh scan)" },
    { "eval_id": 5, "text": "Records exemptPatterns in .github/doc-sweep/docs-ci.json as the built-in default test globs (e.g. **/*.test.*, **/tests/**) FOLLOWED BY the user's addition (package-lock.json), rather than replacing the tests with only the addition", "passed": true, "evidence": "exemptPatterns = the 7 built-in DEFAULT_EXEMPT_PATTERNS in order + package-lock.json appended last" },
    { "eval_id": 5, "text": "Persists the same exemptPatterns list into .claude/context/audience-rules.md, the same way docPatterns/excludeDirs are persisted", "passed": true, "evidence": "audience-rules.md exemptPatterns: block holds the identical 8-item list in the same order" },
    { "eval_id": 5, "text": "Functionally, a PR changing only package-lock.json (or only a test file) passes while a PR changing src code still enforces", "passed": true, "evidence": "end-to-end: lockfile-only exit 0, test-only exit 0, src/app.js exit 1; doc-classify gave {nonDoc:[src/app.js], docChanged:false}" }
  ]
}
