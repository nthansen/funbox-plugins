{
  "skill": "install-docs-ci",
  "pass_rate": 1.0,
  "threshold": 0.9,
  "model": "claude-opus-4-8[1m]",
  "source_hash": "sha256:e87d370898339548785a749f4edfc82a30efbca0129e11acebe5333d2f11e4c5",
  "results": [
    { "eval_id": 1, "text": "Vendors the check script docs-ci-check.sh into the project's .github/doc-sweep/ directory", "passed": true, "evidence": "cp docs-ci-check.sh to .github/doc-sweep/; diff vs bundled empty; executable" },
    { "eval_id": 1, "text": "Also copies doc-classify.mjs alongside docs-ci-check.sh into .github/doc-sweep/ so the classifier resolves at runtime", "passed": true, "evidence": "doc-classify.mjs copied to same dir; diff vs bundled identical" },
    { "eval_id": 1, "text": "For the default doc-file set, writes a config JSON at .github/doc-sweep/docs-ci.json that OMITS docPatterns (the classifier's built-in default applies) and records an excludeDirs array", "passed": true, "evidence": "docs-ci.json = {excludeDirs:[]}; node check: 'docPatterns' in c === false" },
    { "eval_id": 1, "text": "Scaffolds a pull_request workflow at .github/workflows/doc-sweep-docs.yml that runs the vendored check script with checkout fetch-depth:0", "passed": true, "evidence": "workflow has on:pull_request, checkout@v6 fetch-depth:0, run: bash .github/doc-sweep/docs-ci-check.sh .github/doc-sweep/docs-ci.json" },
    { "eval_id": 1, "text": "Prints a structured summary listing the workflow/script/classifier/config paths, the doc-file set, the [skip docs] token, the branch-protection note, and how to edit/uninstall", "passed": true, "evidence": "step-6 template rendered with real paths, Doc-file set: default, Exempt set: default, [skip docs], branch-protection note, edit/uninstall line" },
    { "eval_id": 1, "text": "Does not scaffold anything until the user confirms", "passed": true, "evidence": "step 2 AskUserQuestion precedes the only write steps (4/5); no writes before the confirming prompt" },
    { "eval_id": 1, "text": "Re-running the install does not duplicate the workflow (idempotent)", "passed": true, "evidence": "sha1 of workflow identical before/after; find count stayed 1" },
    { "eval_id": 1, "text": "For the default exempt-set choice, omits exemptPatterns from the config JSON so the classifier's built-in test globs apply", "passed": true, "evidence": "docs-ci.json has only excludeDirs; node check: 'exemptPatterns' in c === false" },
    { "eval_id": 2, "text": "Deletes the scaffolded workflow .github/workflows/doc-sweep-docs.yml", "passed": true, "evidence": "post-uninstall find shows only unrelated ci.yml; workflow gone" },
    { "eval_id": 2, "text": "Deletes the vendored check script, the vendored doc-classify.mjs, and the docs-ci.json config under .github/doc-sweep/", "passed": true, "evidence": "all three rm -f'd; rmdir .github/doc-sweep succeeded (empty)" },
    { "eval_id": 2, "text": "Leaves unrelated workflows and files untouched", "passed": true, "evidence": "sibling ci.yml present and unmodified after uninstall" },
    { "eval_id": 2, "text": "Confirms what was removed (workflow, script, classifier, config paths)", "passed": true, "evidence": "all four removed paths enumerable/named from the pre-uninstall listing" },
    { "eval_id": 3, "text": "Detects the existing install and offers Reconfigure / Uninstall / Cancel rather than a blind fresh install", "passed": true, "evidence": "detection test -f workflow found it → offer-3-choices branch" },
    { "eval_id": 3, "text": "On Reconfigure, rewrites the docs-ci.json config with docPatterns set to the minimal glob list (e.g. CLAUDE*.md and README*.md only) while preserving the other choices", "passed": true, "evidence": "docPatterns rewritten to [**/CLAUDE*.md,**/README*.md]; excludeDirs unchanged" },
    { "eval_id": 3, "text": "Leaves the workflow file in place (rewrites it only if its path or name changed)", "passed": true, "evidence": "sha1 of workflow identical before/after; diff empty" },
    { "eval_id": 3, "text": "On Reconfigure, unconditionally re-copies doc-classify.mjs alongside docs-ci-check.sh into .github/doc-sweep/ (self-heals a missing or stale classifier) even though the workflow file itself is left in place", "passed": true, "evidence": "pre-state omitted classifier + stale script; after reconfigure both diff-identical to bundled (healed)" },
    { "eval_id": 3, "text": "The existing docs-ci.json fixture carries an exemptPatterns of 7 built-in test globs plus one addition; since this reconfigure only changes the doc-file set, the rewritten docs-ci.json preserves that exemptPatterns list unchanged, still starting with all 7 built-ins", "passed": true, "evidence": "post config exemptPatterns.length===8, first 7 === DEFAULT_EXEMPT_PATTERNS in order, JSON.stringify byte-identical to fixture incl. trailing package-lock.json" },
    { "eval_id": 4, "text": "Scans for likely-vendored directories (submodules, non-root package manifests, known vendor names) and presents candidates for confirmation", "passed": true, "evidence": "three signals fired: .gitmodules → third_party/sub; non-root packages/app/package.json; root vendor/ + third_party/" },
    { "eval_id": 4, "text": "Records the confirmed excludeDirs list into the .github/doc-sweep/docs-ci.json config", "passed": true, "evidence": "docs-ci.json excludeDirs=[vendor, third_party, packages/app]; also persisted to audience-rules.md" },
    { "eval_id": 4, "text": "Reads an existing excludeDirs list from .claude/context/audience-rules.md if one is already present rather than re-prompting", "passed": true, "evidence": "pre-existing audience-rules excludeDirs:[vendor] read verbatim → docs-ci.json {excludeDirs:[vendor]}, NOT the fuller fresh-scan set" },
    { "eval_id": 5, "text": "Records exemptPatterns in .github/doc-sweep/docs-ci.json as the built-in default test globs (e.g. **/*.test.*, **/tests/**) FOLLOWED BY the user's addition (package-lock.json), rather than replacing the tests with only the addition", "passed": true, "evidence": "7 built-ins pulled from bundled context/audience-rules.md (== DEFAULT_EXEMPT_PATTERNS); exemptPatterns=[7 builtins, package-lock.json], addition last" },
    { "eval_id": 5, "text": "Persists the same exemptPatterns list into .claude/context/audience-rules.md, the same way docPatterns/excludeDirs are persisted", "passed": true, "evidence": "audience-rules.md exemptPatterns: block holds the identical 8-entry list in the same order" },
    { "eval_id": 5, "text": "Functionally, a PR changing only package-lock.json (or only a test file) passes while a PR changing src code still enforces", "passed": true, "evidence": "doc-classify: lockfile-only & test-only → docChanged:false; src/foo.js → nonDoc:[src/foo.js]. End-to-end docs-ci-check.sh: lockfile branch exit 0, src branch exit 1. Default-config control flags lockfile — proving add-extras caused the pass" }
  ]
}
