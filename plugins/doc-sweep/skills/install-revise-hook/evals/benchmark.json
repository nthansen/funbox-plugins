{
  "skill": "install-revise-hook",
  "pass_rate": 1.0,
  "threshold": 0.9,
  "model": "claude-opus-4-8[1m]",
  "source_hash": "sha256:b4cc05862d30d0ea0f9330123e26ccac218d255466ffdf95bc53e031a0d35df9",
  "results": [
    { "eval_id": 1, "text": "Copies the hook script to a stable path under the project's .claude/hooks/", "passed": true, "evidence": "revise-push-guard.sh copied to .claude/hooks/doc-sweep-revise-push.sh; executable, matches bundled source" },
    { "eval_id": 1, "text": "Also copies doc-classify.mjs into the same directory as the copied hook script so the classifier resolves at runtime", "passed": true, "evidence": "doc-classify.mjs (3422 bytes) copied into the same .claude/hooks/ dir" },
    { "eval_id": 1, "text": "Writes a config JSON capturing docPatterns (a glob list)/repoScope AND trigger:\"commit\" AND an excludeDirs array", "passed": true, "evidence": "doc-sweep-revise.json = {repoScope:doc-sweep-only, trigger:commit, excludeDirs:[]}; docPatterns correctly absent for the default doc-file set" },
    { "eval_id": 1, "text": "Merges a PreToolUse matcher:Bash hook entry into the project settings.json referencing the copied hook + config, preserving any existing settings", "passed": true, "evidence": "settings.json PreToolUse[0] = {matcher:Bash, hooks:[{command:'<abs hook> <abs config>'}]}, paths verified" },
    { "eval_id": 1, "text": "Offers to seed the review marker on this fresh install (seed-now / review-now / leave) rather than silently leaving it unset", "passed": true, "evidence": "seed-now path wrote HEAD sha to .git/doc-sweep-revise-marker; matches git rev-parse HEAD" },
    { "eval_id": 1, "text": "Prints a structured summary listing the settings/hook/classifier/config paths, the trigger, marker state, bypass tokens, and how to edit/uninstall", "passed": true, "evidence": "all step-8 fields present: settings/hook/classifier/config paths, trigger=commit, marker seeded, excludeDirs=(none)" },
    { "eval_id": 1, "text": "Re-running the install does not duplicate the hook entry (idempotent)", "passed": true, "evidence": "re-ran settings-merge; hooks.PreToolUse.length stayed 1" },
    { "eval_id": 1, "text": "For the default exempt-set choice, omits exemptPatterns from the config JSON so the classifier's built-in test globs apply", "passed": true, "evidence": "doc-sweep-revise.json has no exemptPatterns key (only repoScope/trigger/excludeDirs)" },
    { "eval_id": 2, "text": "Removes the guard PreToolUse entry (the one referencing doc-sweep-revise-push.sh) from settings.json", "passed": true, "evidence": "PreToolUse shrank 2→1, retaining only the unrelated echo hook" },
    { "eval_id": 2, "text": "Deletes the copied hook script, the copied doc-classify.mjs next to it, and the config JSON", "passed": true, "evidence": "all three rm -f'd; .claude/hooks empty afterward" },
    { "eval_id": 2, "text": "Leaves unrelated settings (other hooks/keys) untouched", "passed": true, "evidence": "someOtherSetting:keepme and the unrelated PostToolUse hook remain byte-for-byte" },
    { "eval_id": 2, "text": "Leaves the per-clone review marker file in place (does not delete it)", "passed": true, "evidence": ".git/doc-sweep-revise-marker still holds the seeded SHA after uninstall" },
    { "eval_id": 3, "text": "Detects the existing install and offers Reconfigure / Uninstall / Cancel rather than doing a blind fresh install", "passed": true, "evidence": "grep for doc-sweep-revise-push.sh in settings.json matched → detection before the three-choice prompt" },
    { "eval_id": 3, "text": "On Reconfigure, rewrites the config JSON with the new trigger while preserving the other choices", "passed": true, "evidence": "post config = {repoScope:all, trigger:commit, excludeDirs:[vendor]}; trigger push→commit, others preserved" },
    { "eval_id": 3, "text": "Leaves the review marker file untouched during reconfigure (does not re-seed it)", "passed": true, "evidence": "diff of marker pre/post reconfigure identical" },
    { "eval_id": 3, "text": "Updates the settings.json matcher only if the hook path or settings location changed", "passed": true, "evidence": "path/location unchanged → settings.json diff pre/post identical (matcher left as-is)" },
    { "eval_id": 3, "text": "On Reconfigure, unconditionally re-copies doc-classify.mjs next to the hook script (self-heals a missing or stale classifier) even when the hook path itself is unchanged and the hook script is not re-copied", "passed": true, "evidence": "both corrupted pre-reconfigure; after, hook still corrupted (not re-copied) while doc-classify.mjs byte-identical to bundled (self-healed)" },
    { "eval_id": 4, "text": "Scans for likely-vendored directories (submodules, non-root package manifests, known vendor names) and presents candidates for confirmation", "passed": true, "evidence": "three signals fired: .gitmodules → libs/thirdparty; non-root packages/widget/package.json; vendor/ dir" },
    { "eval_id": 4, "text": "Persists the confirmed excludeDirs list into .claude/context/audience-rules.md", "passed": true, "evidence": "audience-rules.md excludeDirs: block lists vendor, libs/thirdparty, packages/widget" },
    { "eval_id": 4, "text": "Mirrors the same excludeDirs into the hook config JSON written next to the hook", "passed": true, "evidence": "user-global doc-sweep-revise.json excludeDirs = [vendor, libs/thirdparty, packages/widget], identical order" },
    { "eval_id": 5, "text": "Records exemptPatterns in doc-sweep-revise.json as the built-in default test globs (e.g. **/*.test.*, **/tests/**) FOLLOWED BY the user's addition (package-lock.json), rather than replacing the tests with only the addition", "passed": true, "evidence": "exemptPatterns = the 7 built-in globs in order + package-lock.json appended last" },
    { "eval_id": 5, "text": "Persists the same exemptPatterns list into .claude/context/audience-rules.md, the same way docPatterns/excludeDirs are persisted", "passed": true, "evidence": "audience-rules.md exemptPatterns: block holds the identical 8-item list in the same order" },
    { "eval_id": 5, "text": "Functionally, with that config, the hook treats a package-lock.json-only change (or a test-only change) as not requiring docs while a src-code change still blocks", "passed": true, "evidence": "doc-classify --config gave {nonDoc:[src/app.js], docChanged:false}; negative control (only package-lock.json, no tests) flagged src/app.test.js too — proving append behavior" }
  ]
}
