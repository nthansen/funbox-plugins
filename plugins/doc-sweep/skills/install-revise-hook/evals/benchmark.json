{
  "skill": "install-revise-hook",
  "pass_rate": 1.0,
  "threshold": 0.9,
  "model": "claude-opus-4-8[1m]",
  "source_hash": "sha256:6f2efe95eda829e876d3652b2468a4861661ae621874a1fe06c70a425a25d3be",
  "results": [
    { "eval_id": 1, "text": "Copies the hook script to a stable path under the project's .claude/hooks/", "passed": true, "evidence": "revise-push-guard.sh copied to .claude/hooks/doc-sweep-revise-push.sh (6662 bytes, executable)" },
    { "eval_id": 1, "text": "Also copies doc-classify.mjs into the same directory as the copied hook script so the classifier resolves at runtime", "passed": true, "evidence": "doc-classify.mjs copied into same .claude/hooks/ dir alongside the hook" },
    { "eval_id": 1, "text": "Writes a config JSON capturing docPatterns (a glob list)/repoScope AND trigger:\"commit\" AND an excludeDirs array", "passed": true, "evidence": "doc-sweep-revise.json = {repoScope:doc-sweep-only, trigger:commit, excludeDirs:[]}; docPatterns correctly absent for default doc-file set" },
    { "eval_id": 1, "text": "Merges a PreToolUse matcher:Bash hook entry into the project settings.json referencing the copied hook + config, preserving any existing settings", "passed": true, "evidence": "pre-seeded permissions.allow + a Write-matcher hook preserved; new Bash matcher appended referencing hook+config" },
    { "eval_id": 1, "text": "Offers to seed the review marker on this fresh install (seed-now / review-now / leave) rather than silently leaving it unset", "passed": true, "evidence": "seed-now path wrote HEAD sha to $(git rev-parse --git-common-dir)/doc-sweep-revise-marker" },
    { "eval_id": 1, "text": "Prints a structured summary listing the settings/hook/classifier/config paths, the trigger, marker state, bypass tokens, and how to edit/uninstall", "passed": true, "evidence": "step-8 summary with concrete settings/hook/classifier/config paths, Trigger: commit, marker seeded, bypass tokens, edit/uninstall line" },
    { "eval_id": 1, "text": "Re-running the install does not duplicate the hook entry (idempotent)", "passed": true, "evidence": "re-ran merge → ALREADY_PRESENT_NO_CHANGE; node count = exactly 1 PreToolUse matcher referencing the hook" },
    { "eval_id": 1, "text": "For the default exempt-set choice, omits exemptPatterns from the config JSON so the classifier's built-in test globs apply", "passed": true, "evidence": "doc-sweep-revise.json has only repoScope/trigger/excludeDirs — no exemptPatterns key" },
    { "eval_id": 2, "text": "Removes the guard PreToolUse entry (the one referencing doc-sweep-revise-push.sh) from settings.json", "passed": true, "evidence": "prior install had 2 matchers; uninstall removed the guard Bash matcher, leaving only the unrelated Write matcher" },
    { "eval_id": 2, "text": "Deletes the copied hook script, the copied doc-classify.mjs next to it, and the config JSON", "passed": true, "evidence": "all three rm -f'd; .claude/hooks empty afterward" },
    { "eval_id": 2, "text": "Leaves unrelated settings (other hooks/keys) untouched", "passed": true, "evidence": "permissions.allow:[Bash(npm test)] and unrelated Write hook preserved byte-for-byte" },
    { "eval_id": 2, "text": "Leaves the per-clone review marker file in place (does not delete it)", "passed": true, "evidence": "marker content identical before/after uninstall (unchanged)" },
    { "eval_id": 3, "text": "Detects the existing install and offers Reconfigure / Uninstall / Cancel rather than doing a blind fresh install", "passed": true, "evidence": "grep for doc-sweep-revise-push.sh in settings.json matched → Reconfigure branch" },
    { "eval_id": 3, "text": "On Reconfigure, rewrites the config JSON with the new trigger while preserving the other choices", "passed": true, "evidence": "fixture {repoScope:all, trigger:push, excludeDirs:[], exemptPatterns:[7+lock]} → after: trigger:commit, repoScope/excludeDirs unchanged" },
    { "eval_id": 3, "text": "Leaves the review marker file untouched during reconfigure (does not re-seed it)", "passed": true, "evidence": "marker content identical before/after reconfigure" },
    { "eval_id": 3, "text": "Updates the settings.json matcher only if the hook path or settings location changed", "passed": true, "evidence": "path/location unchanged → matcher command string identical before/after (not rewritten)" },
    { "eval_id": 3, "text": "On Reconfigure, unconditionally re-copies doc-classify.mjs next to the hook script (self-heals a missing or stale classifier) even when the hook path itself is unchanged and the hook script is not re-copied", "passed": true, "evidence": "doc-classify.mjs mtime changed (re-copied) while hook md5 unchanged (not re-copied)" },
    { "eval_id": 3, "text": "The existing doc-sweep-revise.json fixture carries an exemptPatterns of the 7 built-in test globs plus one addition (e.g. package-lock.json); since this reconfigure only changes the trigger (not the exempt choice), the rewritten config preserves that exemptPatterns list unchanged, still starting with all 7 built-ins", "passed": true, "evidence": "node check: exemptPatterns.length===8, first 7 = built-ins in order, last = package-lock.json — rewritten verbatim alongside the trigger change" },
    { "eval_id": 4, "text": "Scans for likely-vendored directories (submodules, non-root package manifests, known vendor names) and presents candidates for confirmation", "passed": true, "evidence": "three signals fired: .gitmodules → libs/some-submodule; non-root packages/app/package.json; root vendor/" },
    { "eval_id": 4, "text": "Persists the confirmed excludeDirs list into .claude/context/audience-rules.md", "passed": true, "evidence": "audience-rules.md excludeDirs: block lists vendor, libs/some-submodule, packages/app" },
    { "eval_id": 4, "text": "Mirrors the same excludeDirs into the hook config JSON written next to the hook", "passed": true, "evidence": "user-global doc-sweep-revise.json excludeDirs = [vendor, libs/some-submodule, packages/app], identical order" },
    { "eval_id": 5, "text": "Records exemptPatterns in doc-sweep-revise.json as the built-in default test globs (e.g. **/*.test.*, **/tests/**) FOLLOWED BY the user's addition (package-lock.json), rather than replacing the tests with only the addition", "passed": true, "evidence": "7 built-ins sourced from bundled context/audience-rules.md; exemptPatterns=[7 builtins, package-lock.json], addition last" },
    { "eval_id": 5, "text": "Persists the same exemptPatterns list into .claude/context/audience-rules.md, the same way docPatterns/excludeDirs are persisted", "passed": true, "evidence": "audience-rules.md exemptPatterns: block holds the identical 8-entry list, same order" },
    { "eval_id": 5, "text": "Functionally, with that config, the hook treats a package-lock.json-only change (or a test-only change) as not requiring docs while a src-code change still blocks", "passed": true, "evidence": "doc-classify --config: lockfile-only & test-only → docChanged:false; src/index.js → nonDoc:[src/index.js]; no-config control flags package-lock.json — proving add-extras caused the pass" }
  ]
}
